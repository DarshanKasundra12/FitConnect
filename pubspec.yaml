name: fitness_coach
description: A fitness coach app built with Flutter.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.6
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  dio: ^5.3.2
  cached_network_image: ^3.2.3
  carousel_slider: ^4.2.1
  url_launcher: ^6.1.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
  # Comment out the assets section until you create the directory
  # assets:
  #   - assets/images/
